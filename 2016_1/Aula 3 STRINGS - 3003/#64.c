#include<string.h>
#include<stdlib.h>
#include<conio.h>

/* Pedir para o usuário inserir 10 palavras (strings), mostrar na tela a maior. */

main(){

    char palavras[10];
    int i=0;
    printf("Informe a %i palavra: \n",i+1);
    gets(palavras);

}
