#include<stdio.h>
#include<stdlib.h>

/* Crie uma função que receba por parâmetro o nome de uma pessoa cadastrada e a data atual, e retorne a idade dessa pessoa.
 Caso a pessoa não esteja cadastrada, retorne 0. */


 struct data{
    int dia;
    int mes;
    int ano;
 };

 struct dados{
    char nome[50];
    struct data nascimento;
 };

struct dados pessoa[15];

int cadastro(char nome[],struct data d_atual){


    return idade;
}


 main(){

    for
    char nome[50];
    int date;

    printf("Informe o seu nome: \n");
    gets(nome);
    int i = cadastro(&nome,struct data);

 }
