#include<stdio.h>
#include<string.h>
#include<ctype.h>

/* Escreva  um  programa  que  capture  como  uma  string  todo  conteúdo  de  um  arquivo  de  nome escolhido pelo usuário,
então converta todas as letras da string para letras minúsculas. Grave o conteúdo modificado de volta no mesmo arquivo.
O programa deverá ser capaz de guardar a string obtida, independente de seu tamanho (utilize alocação dinâmica) */

main(){

    char string[100];
    char *p;

    FILE *arq;

    arq=fopen(exe2.txt,"r");

    while(feof(arq)==0){
        fgetc(arq);
    p=malloc(sizeof*)
        tolower();
    }
}
